package @NAME@::Apache;

# ----------------------------------------------------------------------------
# Хендлеры Apache для проекта @NAME@
# В этом пакете обязательно должны быть функции child_init(), 
#   request_init(), child_exit().
# ----------------------------------------------------------------------------

use strict;
use vars qw($VERSION @ISA @EXPORT);

use Exporter;
@ISA = qw(Exporter);
$VERSION = '1.0';


# Эта функция будет вызвана в момент создания дочернего процесса Apache
sub child_init {
	my $proto = shift;
}

# Эта функция будет вызвана в момент начала обработки нового запроса
sub request_init {
}

# Эта функция будет вызвана в момент уничтожения дочернего процесса Apache
sub child_exit {
}


1;
