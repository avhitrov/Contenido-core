package @NAME@::Init;

# ----------------------------------------------------------------------------
# Инициализация проекта @NAME@. Инициализация выполняется в момент
#    вызова Contenido::Init->init().
# В этом пакете обязательно должна быть функция init().
# ----------------------------------------------------------------------------

use strict;
use vars qw($VERSION @ISA @EXPORT);

use Exporter;
@ISA = qw(Exporter);
@EXPORT = qw();
$VERSION = '1.0';

use @NAME@::Apache;

# Эта функция инициализирует проектно-зависимые 
sub init {
	return 0;
}

1;
